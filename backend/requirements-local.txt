# PRAHARI - LOCAL development requirements (SQLite, no compilation needed)
# Use this for running locally on any Python 3.10-3.13.
# These are all pure-python or have prebuilt wheels - no C compiler required.
#
#   pip install -r requirements-local.txt
#
# For RAG pipeline (optional but recommended):
#   pip install sentence-transformers faiss-cpu
#
# Without those, RAG falls back to TF-IDF (still works, slightly less accurate).

fastapi
uvicorn[standard]
sqlalchemy
aiosqlite
pydantic
pydantic-settings
python-jose[cryptography]
python-multipart
rapidfuzz
networkx
faker
bcrypt
pytest
scikit-learn
google-generativeai
