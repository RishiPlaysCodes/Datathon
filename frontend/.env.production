# Public backend origin embedded into the Vite production bundle.
# Do not append /api/v1 or /status; the API client adds endpoint paths.
VITE_API_URL=https://prahari-final-50044229424.development.catalystappsail.in
